{
  "$id": "core.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "Core balancing domain (D-10), REPLAN nested tree (Phase 9A): camera, general, phase loop, XP, the hole, lightning strike. Values are the prototype's live tuning, verbatim. Bounds policy: fractions 0-1, costs/counts 0-10000, rounds/levels 0-1000, seconds 0-60, camera zoom factors 0.1-100.",
  "properties": {
    "Camera": {
      "additionalProperties": false,
      "description": "Camera zoom (E-5). Overrides geometry.json's zoom_levels/default fallback at load time (engine/coords/__init__.py::load_coordinate_system) so zoom is a designer-editable balancing tunable, not baked into geometry.json.",
      "properties": {
        "default_zoom": {
          "description": "Zoom factor the camera opens at. Must be a member of zoom_levels (cross-field check enforced at load time via CoordinateSystem.set_zoom, not expressible in schema).",
          "maximum": 100,
          "minimum": 0.1,
          "type": "number"
        },
        "zoom_levels": {
          "description": "The discrete camera zoom factors the wheel steps through, ascending.",
          "items": {
            "description": "One discrete zoom factor.",
            "maximum": 100,
            "minimum": 0.1,
            "type": "number"
          },
          "maxItems": 3,
          "minItems": 3,
          "type": "array",
          "uniqueItems": true
        }
      },
      "required": [
        "default_zoom",
        "zoom_levels"
      ],
      "type": "object"
    },
    "General": {
      "additionalProperties": false,
      "description": "Session-wide constants.",
      "properties": {
        "game_scale_rounds": {
          "description": "Rounds per difficulty era (drives era pacing).",
          "maximum": 1000,
          "minimum": 1,
          "type": "integer"
        },
        "starting_currency": {
          "description": "Love the player starts the game with.",
          "maximum": 10000,
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "game_scale_rounds",
        "starting_currency"
      ],
      "type": "object"
    },
    "LightningStrike": {
      "additionalProperties": false,
      "description": "The click-to-strike active ability. Per-level arrays are index-aligned: level N uses damage[N-1], radius[N-1], cooldown[N-1].",
      "properties": {
        "cooldown": {
          "description": "Cooldown in rounds per level.",
          "items": {
            "description": "Rounds between strikes at this level.",
            "maximum": 1000,
            "minimum": 0,
            "type": "integer"
          },
          "maxItems": 3,
          "minItems": 3,
          "type": "array"
        },
        "damage": {
          "description": "Damage per level (x10 combat scale, D-12).",
          "items": {
            "description": "Lightning damage at this level.",
            "maximum": 100000,
            "minimum": 0,
            "type": "integer"
          },
          "maxItems": 3,
          "minItems": 3,
          "type": "array"
        },
        "max_level": {
          "description": "Maximum lightning level.",
          "maximum": 1000,
          "minimum": 1,
          "type": "integer"
        },
        "radius": {
          "description": "Strike radius in tiles per level.",
          "items": {
            "description": "Lightning radius at this level.",
            "maximum": 1000,
            "minimum": 0,
            "type": "integer"
          },
          "maxItems": 3,
          "minItems": 3,
          "type": "array"
        }
      },
      "required": [
        "cooldown",
        "damage",
        "max_level",
        "radius"
      ],
      "type": "object"
    },
    "PhaseLoop": {
      "additionalProperties": false,
      "description": "Round/phase machine timing.",
      "properties": {
        "income_phase_duration": {
          "description": "Seconds the income phase banner/floaters play before build phase.",
          "maximum": 60,
          "minimum": 0,
          "type": "number"
        },
        "round_end_delay": {
          "description": "Seconds of pause after the last enemy dies before the income phase.",
          "maximum": 60,
          "minimum": 0,
          "type": "number"
        },
        "speed_1_5x_min_round": {
          "description": "First round the 1.5x combat speed button unlocks.",
          "maximum": 1000,
          "minimum": 0,
          "type": "integer"
        },
        "speed_2x_min_round": {
          "description": "First round the 2x combat speed button unlocks.",
          "maximum": 1000,
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "income_phase_duration",
        "round_end_delay",
        "speed_1_5x_min_round",
        "speed_2x_min_round"
      ],
      "type": "object"
    },
    "TheHole": {
      "additionalProperties": false,
      "description": "The base ('the hole') being defended.",
      "properties": {
        "base_hp": {
          "description": "The hole's hit points, in absolute HP - deliberately NOT x10 combat scale (BASE_HP stays 10; prototype exception, D-12).",
          "maximum": 1000,
          "minimum": 1,
          "type": "integer"
        },
        "base_income": {
          "description": "Love the hole itself produces per income phase.",
          "maximum": 10000,
          "minimum": 0,
          "type": "integer"
        },
        "base_kills_enemies": {
          "description": "If true, enemies that reach the hole die in it (dealing their damage).",
          "type": "boolean"
        },
        "base_lives": {
          "description": "Lives before game over — an enemy reaching the hole costs one life and wipes the round.",
          "maximum": 1000,
          "minimum": 1,
          "type": "integer"
        },
        "building_revive": {
          "description": "If true, buildings destroyed during the round revive at the income phase (canonical flag; was BUILDING_REVIVE + FEATURE_BUILDING_REVIVE).",
          "type": "boolean"
        }
      },
      "required": [
        "base_hp",
        "base_income",
        "base_kills_enemies",
        "base_lives",
        "building_revive"
      ],
      "type": "object"
    },
    "Tutorial": {
      "additionalProperties": false,
      "description": "The guided new-player tutorial (D5, planning/TutorialPLAN.md).",
      "properties": {
        "economy_buildings_required": {
          "description": "Number of economy buildings the player must place before the tutorial's first End Turn is allowed / the first-end-turn cutscene fires.",
          "maximum": 10000,
          "minimum": 1,
          "type": "integer"
        }
      },
      "required": [
        "economy_buildings_required"
      ],
      "type": "object"
    },
    "XP": {
      "additionalProperties": false,
      "description": "Village XP and levelling.",
      "properties": {
        "base_income_per_village_level": {
          "description": "Love added to the base income for every village level above 1.",
          "maximum": 100,
          "minimum": 0,
          "type": "integer"
        },
        "levelup_love_reward": {
          "description": "Love granted on each village levelup.",
          "maximum": 10000,
          "minimum": 0,
          "type": "integer"
        },
        "village_xp_base_threshold": {
          "description": "XP needed for the first village level.",
          "maximum": 10000,
          "minimum": 0,
          "type": "integer"
        },
        "village_xp_threshold_inc": {
          "description": "Extra XP needed per subsequent level.",
          "maximum": 10000,
          "minimum": 0,
          "type": "integer"
        },
        "village_xp_threshold_inc_growth": {
          "description": "Growth added to the increment each level (accelerating curve).",
          "maximum": 10000,
          "minimum": 0,
          "type": "integer"
        },
        "xp_from_buildings": {
          "description": "If true, buildings dying grants XP per BuildingsGlobal xp_on_death (canonical flag; was FEATURE_XP_FROM_BUILDINGS).",
          "type": "boolean"
        },
        "xp_on_base_damage_kill": {
          "description": "If true, enemies that die by hitting the base still grant XP.",
          "type": "boolean"
        },
        "xp_per_boss": {
          "description": "XP per boss killed.",
          "maximum": 10000,
          "minimum": 0,
          "type": "integer"
        },
        "xp_per_raider": {
          "description": "XP per raider killed.",
          "maximum": 10000,
          "minimum": 0,
          "type": "integer"
        },
        "xp_per_siege_enemy": {
          "description": "XP per siege cannon killed.",
          "maximum": 10000,
          "minimum": 0,
          "type": "integer"
        },
        "xp_per_standard_enemy": {
          "description": "XP per standard enemy killed.",
          "maximum": 10000,
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "base_income_per_village_level",
        "levelup_love_reward",
        "village_xp_base_threshold",
        "village_xp_threshold_inc",
        "village_xp_threshold_inc_growth",
        "xp_from_buildings",
        "xp_on_base_damage_kill",
        "xp_per_boss",
        "xp_per_raider",
        "xp_per_siege_enemy",
        "xp_per_standard_enemy"
      ],
      "type": "object"
    }
  },
  "required": [
    "Camera",
    "General",
    "LightningStrike",
    "PhaseLoop",
    "TheHole",
    "Tutorial",
    "XP"
  ],
  "title": "core",
  "type": "object"
}
