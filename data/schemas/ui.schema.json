{
  "$id": "ui.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "UI balancing domain (D-10). Phase 4 placeholder - real balance values land with the Phase 9 port.",
  "properties": {
    "_lock": {
      "description": "Branch+lock protocol state (D-11): the literal string UNLOCKED, or {locked_by, since} while a feature<Domain> branch exists. Set by /start-domain and cleared ONLY by /merge-domain (Phase 8, T-1) - never by the editor.",
      "oneOf": [
        {
          "const": "UNLOCKED"
        },
        {
          "additionalProperties": false,
          "properties": {
            "locked_by": {
              "minLength": 1,
              "type": "string"
            },
            "since": {
              "pattern": "^\\d{4}-\\d{2}-\\d{2}",
              "type": "string"
            }
          },
          "required": [
            "locked_by",
            "since"
          ],
          "type": "object"
        }
      ]
    },
    "hud_scale": {
      "description": "HUD scale multiplier; 1.0 = native pixel size. Phase 4 placeholder - real balance values land with the Phase 9 port.",
      "maximum": 2,
      "minimum": 0.5,
      "type": "number"
    },
    "show_fps": {
      "description": "Show the fps counter in the HUD. Phase 4 placeholder - real balance values land with the Phase 9 port.",
      "type": "boolean"
    }
  },
  "required": [
    "_lock",
    "hud_scale",
    "show_fps"
  ],
  "title": "ui",
  "type": "object"
}
